{
  "log": {"level": "error", "timestamp": true},
  "inbounds": [
    {
      "type": "vless",
      "tag": "vless-reality",
      "listen": "::",
      "listen_port": ${VLESS_PORT},
      "users": [{"uuid": "${VLESS_UUID_DEFAULT}"}, {"uuid": "${VLESS_UUID_ADGUARD}"}, {"uuid": "${VLESS_UUID_PROXY}"}],
      "tls": {
        "enabled": true,
        "server_name": "ads.x5.ru",
        "reality": {
          "enabled": true,
          "handshake": {"server": "ads.x5.ru", "server_port": 443},
          "private_key": "${REALITY_PRIVATE_KEY}",
          "short_id": ["${REALITY_SHORT_ID}"]
        }
      }
    },
    {
      "type": "hysteria2",
      "tag": "hysteria2",
      "listen": "::",
      "listen_port": ${HYSTERIA_PORT},
      "users": [{"password": "${HYSTERIA_PASSWORD}"}],
      "tls": {
        "enabled": true,
        "certificate_path": "/etc/sing-box/cert.pem",
        "key_path": "/etc/sing-box/private.key"
      }
    }
  ],
  "outbounds": [
    {
      "type": "wireguard",
      "tag": "warp",
      "server": "engage.cloudflareclient.com",
      "server_port": 2408,
      "local_address": ["${WARP_IPV4}", "${WARP_IPV6}"],
      "private_key": "${WARP_PRIVATE_KEY}",
      "peer_public_key": "${WARP_PEER_PUBLIC_KEY}",
      "mtu": 1280
    },
    {"type": "direct", "tag": "direct"}
  ],
  "route": {"final": "warp"}
}
